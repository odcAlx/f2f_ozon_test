# F2F Bank - Тестовое задание

F2F Bank - простое веб-приложение, имитирующее сервис переводов между пользователями в стиле СБП. Пользователи могут регистрироваться, входить в систему, пополнять баланс и отправлять переводы по номеру телефона

---

## Как запустить приложение

### 1. Перейдите в корень проекта
```bash
cd <папка репозитория>
```
### 2. Поднимите контейнеры Docker
```bash
docker compose up -d --build
```
### 3. Остановите приложение по окончании работы
- Обычная остановка

    ```bash
    docker compose down
    ```
- Полный сброс вместе с данными базы данных

    ```bash
    docker compose down -v
    ```
*Перед запуском следует убедиться, что Docker установлен и запущен Docker Desktop*  
*Приложение будет доступно по адресу [http://localhost](http://localhost)*

## Как запустить тесты

### 1. Установите необходимые зависимости

```bash
npm install
```

### 2. Запустите тесты

- Запустить тесты в фоновом режиме

    ```bash
    npx playwright test
    ```
- Запустить тесты в UI режиме

    ```bash
    npx playwright test -ui
    ```
- Сгенерировать отчет в HTML

    ```bash
    npx playwright show-report
    ```
## Тест-кейсы
<details>
    <summary>1. Регистрация нового пользователя</summary>
    <ul>
        <li><b>Описание:</b> Проверка успешного создания аккаунта с валидными данными (имя, фамилия, email, пароль)</li>
        <li><b>Предусловия:</b> Пользователь находится на странице /register, email свободен (еще не зарегестрирован)</li>
        <li><b>Шаги:</b>
            <ul>
                <li>1. Запонить Имя</li>
                <li>1. Запонить Фамилию</li>
                <li>1. Запонить Email</li>
                <li>1. Запонить Пароль</li>
                <li>1. Нажать "Regiser"</li>
            </ul>
        </li>
        <li><b>Ожидаемый результат:</b> Происходит редирект на страницу /login</li>
        <li><b>Приоритет:</b> Критический</li>
    </ul>
</details>